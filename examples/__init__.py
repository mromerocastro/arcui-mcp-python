# Marker so `examples` is importable as a package when the current working
# directory is the repo root (the documented usage of the templates).
#
# This file deliberately stays empty. The examples are not installed with
# the wheel — they ship in the source repository as standalone templates.
